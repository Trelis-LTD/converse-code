# Maintenance scripts

- `browser_e2e.py` drives the shipped reference page in real Chromium.
- `vendor_converse_sdk.py` refreshes the pinned, licensed Browser SDK bundle.

Run scripts with `uv run scripts/<name>.py`.
